# Laputa local-only artifacts (never committed) — except the config template
.laputa/*
!.laputa/config.yaml.example
*.sqlite
*.sqlite-*

# Laputa compiled graph (reproducible via `laputa compile`; keep schema.json)
graph/facts.jsonl
graph/manifest.json

# Personal trial data (local-only; do not commit — contains personal/work memory)
raw/teams/*-memory/
raw/teams/personal/
.mcp.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
dist/
build/
*.egg-info/
.ruff_cache/
.mypy_cache/
.pytest_cache/

# OS / editors
.DS_Store
.idea/
.vscode/
